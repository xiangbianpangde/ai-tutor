# 2026-06-13 · C2 收尾（M-014 数据管线 + 噪声红线）

> 对应 STATUS 阶段 C · C2 最后一块（v2 #6 数据管线）。**C2 第二波至此 7/7 全完成。** 855 测试全绿。

## M-014（backend/pipeline/）

### NoiseGate（noise_gate.py）—— 验收红线"噪声<5%"成为可查门禁
把 v1 `is_noise_title`（FIX-A/B/K/M 累积的禁类分类器：裸文件名/代码注释/整句话/骨架节名/
时间戳/截断标题…）包成门禁：给概念名算禁类占比 + 对阈值判定。**v1 单一可信源**，换体裁出
新形态即回灌 `is_noise_title`（走向决议 §五-4）。
- `audit(names)` → {total, noise_count, noise_rate, threshold, passed, offenders}
- `audit_kg(store, kg_id)` → 只读审计已建 KG（不改数据）
- 端点 `GET /api/knowledge/graphs/{kg_id}/noise`（只读）

### DataPipeline（data_pipeline.py）—— 采集→清洗→结构化编排
三阶段 + 每阶段可注入 runner（默认包 v1 research/acquisition/kg_enrich adapter；测试注入假 runner，
不碰网络/LLM/演示资产），建图后过 NoiseGate。设计为可经 M-007 TaskManager 后台异步跑（接 ProgressReporter）。

架构：新增 `backend.pipeline ⊥ routers/app/main` import-linter 契约（第 5 契约 KEPT）。

## 验证

```
backend/   58 passed（+7 pipeline）
全量       855 passed（848 + 7，0 回退）
ruff / lint-imports   All passed / 5 kept
真机（只读审计真实 KG）:
  GET /api/knowledge/graphs/aaailunwenyandu-...-kg-full-v1/noise
  → total=16 noise=0 rate=0.0 passed=True   ← 与 FIX-M worklog（0/16）一致，演示资产零触碰
关键测试: 干净集 0% 过线；5清5噪=50% 不过线；阈值边界；audit_kg 读真实概念；KG 不存在→KG_NOT_FOUND；
          DataPipeline 三阶段顺序 + 进度到 100% + 末段噪声审计
```

## C2 第二波完成盘点（7/7）
| # | 模块 | 落地 |
|---|------|------|
| 0 | 波前文档去 sys.path | pdf2zh 集成文档按现实重写 |
| 1 | M-005 CacheLayer | SQLite L1 缓存，跨重启 |
| 2 | M-004 SessionManager | 包 v1 SessionStore + session_summaries |
| 3 | M-007 TaskManager | 线程池后台 + build_kg 异步 |
| 4 | M-006 EventBus | 进程内 pub/sub + recorder |
| 5 | M-017 FileManager | 人类可读目录替 UUID |
| 6 | M-014 数据管线 | NoiseGate 红线 + DataPipeline 编排 |

## 下一步
- **C3 第三波**：M-008 RAG 引擎 + M-009 教学编排（对应 v2 #9 AgenticRAG + #10 部分）。C3 完成触发 **D2 收束节点**。
- C2 真实端到端 demo（采集→清洗→建图全链 + 噪声<5%）需牺牲品语料（不碰演示库）；default 阶段 runner 接 v1 adapter 的实跑随 demo。
