# 2026-06-12 · 22 问题根因调查与修复

## 背景

用户列出 22 个真实使用中发现的问题（原 `doc/发现问题.txt` 21 条 + 新增 #22「每个 markdown 标题当一个 concept」），要求逐个深挖根因后针对根因修复。

## 做了什么

### 1. 根因调查（4 个问题簇全部进代码 + 现场取证）

取证范围：v1 全部相关源码、`data/`（含 ACP 真实语料与 tutor.db）、`C:/Users/yhn/Desktop/学习文件集合/ACP学习/`（用户实际使用产物）。

关键证据：
- ACP KG 356 概念中 **126 个（35%）是噪声**：来源文件名（`01-edu.aliyun.com-087e03`）、代码块内 Python 注释（`# ====` ×8）、整句话——`_iter_headings` 不跳代码围栏 + 合并用文件名当标题（#22/#1）
- `00_merged_complete.md` 实测 **75,393 行**直接交给学习者读；121 份拆分是 Claude Desktop 人工补救（#2/#8）
- `engine.respond()` 发 `answered`，reduction PRACTICE 只认 `practice_done` → **死循环**，实测 15 步教学 14 步连发 give_exercise、只"学完"1 个概念=文档标题（#16/#20）
- 判分器 `score(concept, answer)` **看不到题目** → 套话答案判 correct +0.298 → 掌握度 0.943（#18/#20）
- `acquisition_adapter.py:224` 多源采集 KG 只吃第一个源 → 用户被迫手写合并脚本（#8/#14）
- `select_system_action` 无任何墙钟逻辑；`total_elapsed_min`/`active_time_min` 字段存在但无人写（#10）
- feynman 选择门槛 `self_assessment_accuracy < 0.5` 在默认画像 0.6 下数学上不可达（#12）
- 项目自己的 acp 脚本全部 `getattr(x,'fn',x)` 拆包绕开 MCP——#4「弃用 MCP」实锤；build_kg(full) 数百次 LLM 调用塞单个 tool call 必超时（#4/#21）

22 问题收敛为 5 条系统性根因（R1 语料缺整理环节 / R2 标题当概念 / R3 引擎↔宿主契约模糊 / R4 MCP 长事务不匹配 / R5 横切关注点有 schema 无接线）。

**报告落盘**：`doc/reports/根因分析-22问题.md`（每问题：现象→根因 file:line→修复→落点）。

### 2. v1 修复（10 项，FIX-A~J）

详见 STATUS.md 同日条目。要点：知识管线 3 项（标题治理/合并治理/多源合并）、引擎 4 项（判分看题/PRACTICE 死循环/时长休息/feynman 可达）、PDF 语种收尾、deepen 多面采集开关、study_pack 学习材料分片产物。

### 3. 计划回灌

- `doc/plan/开发清单.md`：追加 #22 覆盖行 + 3 条验收红线（KG 噪声率<5% / respond 必可推进 / 判分必含题目 / 可视化必答学习者问题）
- `CLAUDE.md`：阶段措辞更新（v1 修复迭代 + v2 待 review）、21→22 对照表
- `doc/发现问题.txt`：补第 22 条

## 验证

- 全量回归：**786 passed**（基线 672）
- 真实语料冒烟：ACP 75,393 行 md 重过新 `_build_toc`——概念 356→211，文件名/分隔线/URL/整句噪声 126→**0**
- 死循环回归：PRACTICE 收到 answered(correct) → NEXT（测试覆盖）

## 遗留

- 架构级问题（#3 可视化 / #4 MCP / #5 文件布局 / #6 会话记忆 / #7 推送 / #11 缓存监控 / #15 RAG 闭环 / #21 直连）：根因在 v1 架构，按计划落 v2，本轮不动
- FIX-I（deepen）端到端验证需 `uv sync --extra research` 后真实跑一轮采集
- 跨源同名概念去重：v1 只告警（quality warnings），内容级归并属 v2 数据管线（M-014）
