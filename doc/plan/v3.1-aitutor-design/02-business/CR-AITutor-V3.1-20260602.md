# 澄清请求清单 — AITutor V3.1（终版）

> 编号：CR-001
> 阻塞状态：0 条阻塞 / 1 条可绕过
> 本轮 V3.1 = 0 新增 CR（V3.0 的 10 条 OC 中 3 条已决策 + 1 条 PM 决策保留 + 6 条非阻塞；AR-017 备选方案为可绕过 CR）

---

## CR-001 [可绕过] 长期记忆实体淘汰策略备选方案

- **关联歧义**：AR-017
- **关联 PRD**：F-010
- **歧义描述**：长期记忆实体满时，淘汰策略选 LRU 还是重要性评分？
- **SA 建议方案**：**LRU 淘汰**（依据：标准缓存惯例 + DE-032 已有 created_at 字段）
  - 置信度：中
  - 实现：按 `created_at` 升序淘汰最旧 10%
- **备选方案**：**重要性评分淘汰**（按实体被引用频次 + 实体类型权重）
  - 实现：保留 P0 实体（用户画像/进度/关键事实）→ 淘汰临时实体
  - 缺点：需新增 `importance_score` 字段 + 定期重算
- **需要确认的问题**：
  - [ ] 是否接受 LRU 淘汰作为本期默认？
  - [ ] 重要性评分是否进 backlog（B-014）？
- **风险等级**：低（淘汰策略不影响主流程功能正确性，仅影响"长期记忆"边界体验）
- **阻塞状态**：**可绕过**（SA 已按 LRU 实现，PM 评审若选重要性评分再重构）
- **来源标注**：[PRD:F-010] + [SA推断:依据=标准缓存惯例] + [反例验证:CE-007 长期记忆数据孤岛]
- **建议处理**：在 PM 评审会上同步确认；无需回退 SA-001 当前产出

---

## 已闭环的澄清项（沿用 V2.0 / 已在 AR 中标注"已决策"）

- ~~OC-1 GUI 形态~~ → **保留为 PM 决策项**（AR-001），不阻塞
- ~~OC-4 research-tool/pdf2zh 集成~~ → ✅ 已决策（pip install -e）
- ~~OC-10 状态机 vs LLM 比例~~ → ✅ 已决策（90% / 10%）

---

## SA 洞察

1. **【CR 收敛成果】** 本轮 V3.1 SA 分析产出的澄清请求仅 1 条，且为可绕过状态——说明 V3.0 主体 + V3.1 三条显式化增强（S-020/021/022）已把"开发规范红线"这个高歧义区域完全闭环。
2. **【OC-1 不阻塞】** 唯一保留的 PM 决策项（GUI 形态）已在 SA 阶段以"可替换层抽象"处理（BP-005/BP-010 中"桌面端"均通过统一抽象访问），不阻塞当前 SA 产出。
3. **【阻塞项=0 价值】** CTI 可达 ≥ 0.90，无须回退 PM-001。

[阶梯退出检查] ①CR 数量 ≤ 10: 是（1） ②阻塞项=0: 是 ③D6: 100%
