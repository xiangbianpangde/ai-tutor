# 48 小时学习系统运行模拟（深化版）

> 文件定位：在全部 10 项优化 + 降阶法策略 + 心流调节器实现后的系统运行推演  
> 模拟科目：《同济版高等数学下册》第 8 章 多元函数微分学（一个完整章节里程碑）  
> 学生画像：有一元微积分基础，初次接触多元  
> 对照设计源：`ai-tutor-system-design/data-flow-48h.md`，`doc/降阶学习法.md`

---

## 1. 总览：一次里程碑的完整周期

一个"里程碑"（章节）从进入到完成的完整路径：

```
[进入章节]
  │
  ├─ 章节概览（30s）
  │   系统说明本章位置、前置依赖、最终目标
  │
  ├─ 概念 1 ── 降阶法 4 步 ──→ 知识骨架
  ├─ 概念 2 ── 降阶法 4 步 ──→ 知识骨架
  ├─ ...
  │   心流调节器持续监控，每步动态调参
  │
  ├─ 章节中点检查（可选）
  │   学生自评 vs BKT → 校准元认知
  │
  ├─ 概念 N ── 降阶法 4 步 ──→ 知识骨架
  │
  ├─ 章节出口验证
  │   综合题 → 错误回溯 → 知识骨架汇总
  │
  └─ [里程碑完成]
      产出：本章所有概念的知识骨架 + BKT 掌握度
```

---

## 2. 第 8 章 多元函数微分学 — 完整教学记录

### 2.1 学习前状态

**系统摸底结果（来自 Day 1 冷启动）：**

```
已掌握（mastery > 0.85）:
- 空间直角坐标系（skip）
- 向量及其线性运算（skip）

待学:
- 8.1 多元函数的基本概念（3 个概念）: mastery=0.05
- 8.2 偏导数（3 个概念）        : mastery=0.05 ... *全新*
- 8.3 全微分
- 8.4 链式法则
- 8.5 隐函数求导
- 8.6 几何应用
- 8.7 方向导数与梯度
- 8.8 极值

个人画像：
  preferred_pace = "thorough"
  abstract_tolerance = 0.62
  self_assessment_accuracy = 0.55（偏低，需要校准）
```

---

### 2.2 进入章节（第 1 天下午，Phase 2 开端）

```
系统（章节概览）：
"第 8 章 多元函数微分学。这是从一元到多元的跨越。
你的一元微积分基础扎实——这章就是把导数、微分的概念
从'一条线'扩展到'一个面'。

本章 8 节、20 个新概念。预计 2.5 小时。
每完成一个概念产出知识骨架。本章完成后你能：
1) 算任意多元函数的偏导数
2) 判断函数是否可微
3) 用梯度找极值方向

准备好了就开始。"

学生："开始。"
```

---

### 2.3 概念 1：多元函数的极限（降阶法流程，~12 分钟）

#### 阶段 1：GOAL — 定义高阶目标

```
系统（scaffold_level=2，心流 SHALLOW 初始态）：
"我们先看第一个概念：多元函数的极限。
你学过一元极限：x→x₀ 时 f(x)→A。
多元极限就是把'x→x₀ 这条线'扩展为'(x,y)→(x₀,y₀) 这个面'。
学完这个，你就能判断一个二元函数在某点是否有极限——
这是后续偏导数、连续性、可微性的基础。"

学生："明白了，把一元极限推广到二维。"
→ event: goal_understood → 进入 REDUCE
```

#### 阶段 2：REDUCE — 降维成原子问题

```
系统（sub_step_count=3，调节器根据初始心流 SHALLOW 推荐）：
"好，我们先拆成 3 个原子问题，一个一个来。"

# 原子 1/3
系统："原子问题 1：二元极限中 (x,y)→(x₀,y₀) 和一元的
x→x₀ 最根本的区别是什么？一句话。"
学生："一元只在一条线上靠近，二元可以从四面八方靠近。"
系统："对。'路径'这个概念是核心。下一个。"

# 原子 2/3
系统："原子问题 2：如果沿两条不同路径趋近，
得到的极限值不相同——极限存在吗？"
学生："不存在。极限必须唯一。"
系统："正确。如果 A 路径给 2，B 路径给 3，
整体极限就不存在。记住了？"
→ confirm_each_step=True 时的追问确认

# 原子 3/3  
系统："原子问题 3：判断二元极限不存在，
最常用的方法是什么？"
学生："找两条不同的路径，算出来的极限值不一样。"
系统："哪种路径最容易算？y=kx 这种直线路径？还是曲线？"
学生："直线路径 y=kx 最常用，代入化简看结果是否含 k。"
→ 学生答对了，但系统追加追问（scaffold_level=2 行为）

→ 3 个原子全部 correct → all_atoms_understood → 进入 COMPLEMENT
```

#### 阶段 3：COMPLEMENT — 补集枚举

```
系统（error_count=5，调节器推荐）：
"你理解得不错。现在我们来排除几个常见的错误理解。
我说一个错误观点，你告诉我它错在哪。"

❌ 错误 1/5："二元函数要想有极限，函数必须在这一点有定义。"
学生："不对。一元极限就不要求函数在该点有定义，
多元也一样。极限看的是'趋近'不是'等于'。"

❌ 错误 2/5："沿 x 轴和沿 y 轴趋近结果相同 → 极限存在。"
学生："不充分。只验证了两条路径。
极限存在要求 ALL 路径一致。
比如沿 y=x 或 y=x² 可能得到不同值。"

❌ 错误 3/5："极限值是 f(x₀,y₀)，如果 f 在该点有定义的话。"
学生："不是。极限值是趋近的值，不是函数值。
它们可以不同，这叫可去间断。"

❌ 错误 4/5："判断极限不存在必须用 y=kx 路径。"
学生："不对。有些函数沿 y=kx 所有直线路径都相等，
但沿 y=x² 这样的曲线路径不同。
所以必须考虑非直线路径。"

❌ 错误 5/5："如果沿 y=kx 结果与 k 有关，极限不存在；
如果与 k 无关，极限存在。"
学生："前半对后半错。与 k 无关只是必要条件，
还得验证所有路径。与 k 无关不一定极限存在。"

→ 全部排除 → all_errors_excluded → 进入 RECONSTRUCT
```

#### 阶段 4：RECONSTRUCT — 骨架重构

```
系统："现在把多元函数极限的知识骨架搭起来。
回答四个问题："

系统："前提：什么条件下我们才能讨论极限？"
学生："点 (x₀,y₀) 必须是定义域的聚点，
即任意邻域内都有定义域中的点。"

系统："逻辑：判断的核心推理是什么？"
学生："看沿所有可能的路径趋近 (x₀,y₀) 时，
函数值是否都趋近同一个值。
是 → 极限存在；否 → 极限不存在。"

系统："结论：定义式的核心。"
学生："lim_{(x,y)→(x₀,y₀)} f(x,y) = A。
对任意 ε>0，存在 δ>0，
当 0<√((x-x₀)²+(y-y₀)²)<δ 时，
|f(x,y)-A|<ε。"

系统："易错点在哪？"
学生："最容易犯的错误是：
1) 只验证两条路径就下结论
2) 把函数值当极限值
3) 沿 y=kx 与 k 无关就说极限存在"

系统（展示骨架）：
"┌──────────────────────────────────────────────────┐
│ 多元函数极限                                          │
│ 前提：(x₀,y₀) 是聚点，函数在该点附近有定义                │
│ 逻辑：所有路径趋近同一值 → 存在；任一不同 → 不存在         │
│ 结论：∀ε>0, ∃δ>0, 0<√((x-x₀)²+(y-y₀)²)<δ ⇒ |f(x,y)-A|<ε │
│ 易错点：路径不全、混淆函数值与极限值、y=kx 陷阱             │
└──────────────────────────────────────────────────┘
准确吗？"

学生："准确。"
→ event: reconstructed → 进入 REVIEW
```

#### 阶段 5：REVIEW — 当堂验证

```
系统（difficulty_multiplier=1.0，标准难度）：
"f(x,y) = (x²y)/(x⁴ + y²)
判断 lim_{(x,y)→(0,0)} f(x,y) 是否存在。"

学生思考后回答：
"沿 y=kx：= (x²·kx)/(x⁴ + k²x²) = (kx³)/(x⁴+k²x²) 
               = (kx)/(x²+k²) → 0 （当 x→0）
沿 y=x²：= (x²·x²)/(x⁴ + x⁴) = x⁴/(2x⁴) = 1/2
0 ≠ 1/2，极限不存在。"

系统："分析完整。你不仅算了直线路径，
还主动试了曲线路径——这说明你记住了补集的教训。
mastery 更新：0.05 → 0.67"
→ review_passed → 进入 NEXT
```

#### 心流信号更新（第 1 概念完成后）

```
信号记录：
  answer_length_growth: true（简短回答 → 完整分析，明显变长）
  depth_increasing: true（主动提到"补集的教训"）
  hesitation_decreasing: true（从犹豫到流利）
  self_correction_quality: false（没自纠，但答对了不需要）

净分 = 3 → 下一个概念的 pace 将保持 SHALLOW（0.8x 难度，2 级脚手架）
```

---

### 2.4 概念 2~5：连续、偏导数入门、偏导数计算、高阶偏导数

（篇幅所限，完整记录略。以下是关键转折点记录）

---

```
概念 2（连续）：顺利通过。骨架明确：连续=极限值=函数值。
概念 3（偏导数入门）：学生首次接触 ∂ 符号 → GOAL 阶段花了额外时间。

[心流变化]
概念 2-3 期间学生保持稳定，flow_level = SHALLOW~FLUENT。
第 3 概念 REDUCE 阶段正确率 100% → 调节器微调：
  sub_step_count: 4 → 5（增加挑战）
  confirm_each_step: True → True（保持确认）

概念 4（偏导数计算）：这是本章核心技能。
REDUCE 阶段 5 个原子问题，学生最后 1 个答错：

系统："∂/∂x [sin(x²y)] 怎么算？"
学生："cos(x²y)·2xy"
系统："检查一下：对 x 求偏导时，y 是常数。
外层导数 cos(x²y)，内层 2xy——等一下，
内层对 x 求导是 2xy 吗？
sin(x²y) 的内层函数是 x²y，对 x 求导：2xy。
所以结果是 cos(x²y)·2xy。是对的。"
→ event: answered(correctness=correct) 
   但实际上学生发现自己的错误并纠正了
→ _atom_index 推进
→ 记录 self_correction_quality=true（重要信号）

心流信号更新：
  self_correction_quality: true（自纠 → 深度理解标志）
  净分 = +3 → flow_level 升到 FLUENT
  调节器响应：difficulty_multiplier 1.0→1.2，
  下一个概念的 scaffold_level 1→0
```

---

### 2.5 概念 6：全微分 — 降阶法 + 心流挑战

此时心流 FLUENT，调节器设 difficulty_multiplier=1.2，scaffold_level=0。

#### GOAL 阶段

```
系统（scaffold_level=0，简洁版）：
"全微分。学完这个你能回答：
为什么偏导数存在不足以保证函数可微。
什么条件下偏导数存在+连续 → 可微。"

学生："好，开始。"
```

#### REDUCE 阶段

```
系统（sub_step_count=5，无脚手架，无逐条确认）：
"5 个原子问题，一次答完。"

原子 1：一元微分 dy = f'(x)dx 的几何意义是什么？
原子 2：全微分的定义 dz = ? 
原子 3：全微分 dz 和偏导数 ∂z/∂x, ∂z/∂y 的关系
原子 4：为什么偏导数存在不能推出可微？
原子 5：什么条件下一定能推出可微？

学生逐条作答：

"1: dy 是切线增量，Δy 是曲线实际增量，
dy ≈ Δy 的线性逼近。"

"2: dz = (∂z/∂x)dx + (∂z/∂y)dy。
即两个偏导方向的变化量叠加。"

"3: ∂z/∂x 是沿 x 方向的变化率乘以 dx，
∂z/∂y 是沿 y 方向的变化率乘以 dy，
加起来就是总变化的全微分。"

"4: 偏导数存在只保证沿坐标轴方向的变化有线性逼近。
但沿其他方向（比如对角线）可能不可导。
一元只有两个方向（左/右），多元有无穷多个方向。"

"5: 如果偏导数连续，则可微。
这是因为连续保证局部稳定性，
使得沿任意方向的变化都可控。"

→ 5/5 correct，无 confirm（scaffold_level=0 跳过追问）
→ 进入 COMPLEMENT
```

#### COMPLEMENT 阶段（心流 FLUENT→DEEP 的关键转折）

```
系统（error_count=8，无脚手架）：
"排除以下 8 个错误。有些是细微的，注意辨析。"

❌ 1/8："可微就是偏导数存在，反过来也是。"
❌ 2/8："全微分 dz 就是 Δz，两者没区别。"
❌ 3/8："如果偏导数都连续，不用检验可微性——一定可微。"
❌ 4/8："一个函数如果连续，就一定可微。"
❌ 5/8："全微分 dz 就是 ∂z/∂x·dx 和 ∂z/∂y·dy 的简单相加。"
❌ 6/8："可微函数一定连续。反过来也对。"
❌ 7/8："偏导数不连续就一定不可微。"
❌ 8/8："全微分只适用于二元函数。"

学生回答节选：
"3：前半对后半错。偏导数连续 → 可微是充分条件，
但说'不用检验'不对。实际上可微有严格定义式，
偏导连续只是保证它成立的一个条件。"

"6：可微→连续是对的（可微推出更强的性质）。
但连续→可微不对。连续只保证不断裂，
不保证有线性逼近。就像山路连续但凹凸不平。"

→ 7/8 正确，1 个 partial（错误 5 需要系统补充解释）
→ 调节器检测：error_count 从 8 正常推进，
   flow_level 稳定在 FLUENT，净分 0（没有上升也没下降）
```

```
[心流观察点]
8 个错误中 7 个正确排除，1 个 partial——
学生在错误 5 上卡了一下，但系统补充解析后快速理解。
这说明难度刚好在边界上——挑战略高于当前技能，
但学生能突破。这是心流理论说的 optimal challenge。

flow_level 净分 0（没有正信号也没有负信号）
→ 保持 FLUENT，不升不降
→ 调节器维持 difficulty=1.2，scaffold=0
```

#### RECONSTRUCT 阶段

```
学生产出的骨架：

┌────────────────────────────────────────────────────────┐
│全微分                                                      │
│前提：函数 z=f(x,y) 在 (x₀,y₀) 的邻域内有定义                 │
│逻辑：Δz = [f(x₀+Δx,y₀+Δy)-f(x₀,y₀)]                         │
│      = A·Δx + B·Δy + o(√(Δx²+Δy²))                         │
│      其中 A=∂z/∂x, B=∂z/∂y                                   │
│结论：可微 ⇔ Δz 可以分解为线性部分 + 高阶无穷小                   │
│偏导连续 ⇒ 可微（充分条件）                                    │
│可微 ⇒ 连续（必要条件）                                        │
│可微 ⇒ 偏导存在（必要条件）                                    │
│易错点："偏导存在⇒可微"是错的；"连续⇒可微"也是错的；              │
│        偏导连续是最常用的可微判别法                              │
└────────────────────────────────────────────────────────┘

系统："这个骨架完整。特别注意到你把
三个方向的关系（可微、连续、偏导存在）画成推理链——
这不是教给你的，是你自己整理的。这很好。"
```

#### REVIEW 阶段

```
系统（difficulty_multiplier=1.2，挑战级验证）：
"f(x,y) = 
  { (x³y - xy³)/(x²+y²),  (x,y)≠(0,0)
  { 0,                   (x,y)=(0,0)
  
分析：在 (0,0) 处的偏导数是否存、函数是否可微。"

学生：
"先算偏导：
∂f/∂x(0,0) = lim(h→0) [f(h,0)-f(0,0)]/h 
           = lim(h→0) [(h³·0 - h·0³)/(h²+0²) - 0]/h
           = 0/h = 0
∂f/∂y(0,0) 对称地为 0。
偏导存在（都为 0）。

现在看是否能写成 Δz = A·Δx + B·Δy + o(ρ)：
A=0, B=0, ρ=√(Δx²+Δy²)
需要验证 [f(Δx,Δy)-f(0,0) - 0·Δx - 0·Δy]/ρ → 0？

f(Δx,Δy) = (Δx³·Δy - Δx·Δy³)/(Δx²+Δy²)
需要这个除以 ρ = √(Δx²+Δy²) 是否 → 0。

沿 Δy=k·Δx：
分子 = Δx⁴k - Δx⁴k³ = Δx⁴k(1-k²)
分母 = Δx²(1+k²) × |Δx|√(1+k²) = |Δx|³(1+k²)³ᐟ²
比值 = [Δx⁴k(1-k²)] / [|Δx|³(1+k²)³ᐟ²]
     = |Δx| · k(1-k²)/(1+k²)³ᐟ²

当 Δx→0，比值 → 0。与 k 无关。
所以可微。验证了可微的定义。"

系统："完整分析。你用了先算偏导 → 再验证定义的标准路径。
特别注意到你主动检验了沿任意路径（k 任意）。
这道题是经典——就是考察偏导存在但不一定可微的陷阱，
而这里恰好成立。

skill delta: 从算偏导到验证可微性，这个路径独立完成了。
mastery: 0.05 → 0.78"
```

---

### 2.6 概念 7~12：链式法则、隐函数、几何应用

（教学记录略。以下为心流追踪日志摘要）

```
概念 7（链式法则）:
  REDUCE 阶段顺利，原子问题聚焦"不同变量分支怎么画树状图"
  COMPLEMENT 阶段重点排除"把链式法则当简单相乘"的错误
  时间：1.5x 常规（因为涉及复合结构，分支多）
  心流：FLUENT→DEEP（学生开始主动画变量关系树）
  mastery: 0.05→0.74

概念 8（隐函数求导）:
  REDUCE 阶段出现 1 次 partial（隐函数定理的条件记混了）
  但 COMPLEMENT 阶段正确排除所有错误
  心流：保持在 DEEP
  mastery: 0.05→0.71

概念 9-10（几何应用：空间曲线的切线与法平面）:
  空间想象力要求高 → flow_level 短暂降到 SHALLOW
  调节器响应：difficulty 从 1.2 降回 0.8，scaffold 升到 2
  → 第 10 概念后半段回升到 FLUENT
  mastery: 平均 0.05→0.68（稍低，几何应用需要空间思维）

概念 11（方向导数与梯度）:
  DEEP 心流，学生主动问"梯度是不是就是机器学习里那个？"
  → initiative_taking=true，这是心流的强正信号
  → 系统做了一次打断处理（intent: concept_question）
  → 简短回应后回到教学
  调节器：difficulty 升到 1.4（IMMERSED 级别）
  mastery: 0.05→0.82（本章最高——因为兴趣驱动）

概念 12（极值）:
  COMPLEMENT 阶段排除"极值点处偏导必为零"的误区
  → 学生发现"偏导为零只是必要条件，鞍点也是驻点"
  骨架重构特别强调边界条件和拉格朗日乘数法
  mastery: 0.05→0.69
```

---

### 2.7 章节中点检查（发生在概念 6 完成后）

```
系统："你已经学完本章一半概念（6/12）。
做一个简短的自评：你觉得目前对偏微分学的掌握如何？"

学生："差不多七八成吧。全微分那部分比我想的深，
但原子问题拆开之后清晰了很多。"

系统（LLM 自评解析）：
  自评 ≈ 0.75
  实际 avg mastery（BKT）= 0.68
  差距 = 0.07（在 ±0.15 正常范围内 → 元认知校准良好）

"你的自评和实际相差不大（0.75 vs 0.68），
元认知校准还不错。需要特别关注的是几何应用部分——
那两块概念 mastery 不太稳。后面学完链式法则后
我会帮你自动穿插复习。"
```

---

### 2.8 章节出口验证（里程碑完成）

```
本章 12 个概念全部学完，系统进入出口验证：

系统（综合题）：
"给你一个场景：f(x,y)=x³-3xy+y³
1) 计算一阶偏导
2) 找驻点  
3) 判断每个驻点是极大、极小还是鞍点

这是本章所有技能的整合检验。"

学生逐步作答：

"1) ∂f/∂x = 3x²-3y, ∂f/∂y = -3x+3y²

2) 令两偏导为 0:
   3x²-3y=0 → y=x²
   -3x+3y²=0 → y²=x
   代入：x⁴=x → x(x³-1)=0 → x=0 或 x=1
   (0,0), (1,1)

3) A=∂²f/∂x²=6x, B=∂²f/∂x∂y=-3, C=∂²f/∂y²=6y
   Δ=AC-B²
   
   在 (0,0): A=0, Δ=0·0-9=-9<0 → 鞍点
   在 (1,1): A=6, Δ=36-9=27>0 → f(1,1)=-1 极小值"

系统："全部正确。你走完了偏微分学的完整推理链：
偏导 → 驻点 → 二阶判别 → 极值判定。
这和本章的章节概览目标一致——你做到了。

出口状态：
  本章概念：12/12 完成
  掌握度分布：mastery>0.85: 8个, mastery>0.6: 4个, mastery<0.4: 0个
  平均 mastery: 0.74
  耗时：2h48min（预计 2.5h，偏差 +7%）
  
  知识骨架汇总已持久化。

下一个里程碑：第 9 章 重积分。
需要先做 5 分钟复习（隐函数求导已到期）。
"
```

---

## 3. 心流调节器全流程日志（本章）

下表展示降阶法与心流调节器在整章教学中的协同工作：

```
时间线   | 概念      | FlowLevel | PaceConfig (调控参数)    | 学生表现
─────────┼───────────┼───────────┼─────────────────────────┼─────────────────────
T+0min   | 极限      | SHALLOW   | diff=0.8, scaffold=2   | 正确但犹豫
T+12min  | 极限完成  | →FLUENT   | —                       | 主动自学曲线路径
T+15min  | 连续      | FLUENT    | diff=1.0, scaffold=1   | 标准节奏
T+30min  | 偏导入门  | →SHALLOW  | diff=0.8, scaffold=2   | ∂符号陌生，降难度
T+45min  | 偏导入门✓ | →FLUENT   | diff=1.0, scaffold=1   | 上手后快速
T+60min  | 偏导计算  | FLUENT    | diff=1.2, scaffold=0   | 自纠一次 → 正信号
T+75min  | 偏导计算✓ | →DEEP     | diff=1.2, scaffold=0   | 流畅，自纠
T+80min  | 全微分    | DEEP      | diff=1.2, scaffold=0   | 挑战级验证题一次过
T+100min | 全微分✓   | FLUENT→   | diff=1.2, scaffold=0   | 骨架自建分析链
T+110min | 链式法则  | DEEP      | diff=1.2, scaffold=0   | 主动画变量树
T+130min | 隐函数    | DEEP      | diff=1.2, scaffold=0   | 1次partial→自恢复
T+150min | 几何应用  | →SHALLOW  | diff=0.8, scaffold=2   | 空间想象吃力
T+165min | 几何应用✓ | →FLUENT   | diff=1.0, scaffold=1   | 回归稳定
T+170min | 方向导数  | DEEP→     | diff=1.4, scaffold=0   | 主动跨科联想(ML)
T+185min | 梯度✓     | IMMERSED  | diff=1.4, scaffold=0   | 巅峰体验
T+190min | 极值      | FLUENT    | diff=1.0, scaffold=1   | 稳定收尾
T+200min | 极值✓     | FLUENT    | diff=1.0, scaffold=1   | 完成
T+210min | 出口验证  | —         | —                      | 综合题全对
```

**关键观察：**

1. **调节器降了 2 次难度**（偏导入门、几何应用）——都是在学生遇到陌生符号/需要空间想象时主动降低挑战，防止心流断裂
2. **调节器升了 2 次难度**（偏导计算、梯度+ML联想）——在正信号积累足够后增加挑战深度
3. **一旦调节失误会怎样**：如果在几何应用时不降难度（保持 diff=1.2），学生可能连续受挫 → withdrawal_pattern → 心流跌到 SILENT → 需要 gain_loop_repair。调节器的价值就在于此——**在心流断裂之前主动调整**
4. **降阶法 + 调节器协同**：降阶法的 REDUCE 阶段保证了"原子问题难度可控"；COMPLEMENT 阶段保证了"错误是被排除的而不是被纠正的"——这让调节器的参数调整有精确的调节对象

---

## 4. 心流断裂与修复场景模拟

### 4.1 场景：调节器来不及反应（第 9 章 重积分）

```
第 9 章第 1 概念：二重积分的定义

学生此前连续 8 小时学习（第 4 天末尾），
认知负荷 0.72（接近阈值偏高）。

系统（调节器输入：flow_level=FLUENT, cognitive_load=0.72）：
  → 认知负荷 > 0.7 → 触发修正
  → diff: 1.0→0.85, scaffold: 1→2（降低难度，增加脚手架）

但学生已经疲惫了，不管怎么调都在走神：

信号监测（3 轮后）:
  withdrawal_pattern: true（连续>3 轮极短回答）
  emotional_flatness: true（无情绪标记）
  answer_regression: true（连续答错）

gain_loop_monitor 检测到 "student_withdrawal" 断裂。
→ 修复 action（rebuild_safety）:

"我注意到你今天的精力状态不太一样。
我们已经连续学了 8 个小时——这个进度已经很好了。
今天先到这里，我们休息。

明天开始的 15 分钟，我会帮你快速回顾二重积分的定义，
然后继续。"
```

### 4.2 场景：降阶法消除抗拒（第 12 章 级数）

```
学生对"级数"有畏难心理（摸底时 self-report）。
进入本章时 flow_level = SILENT（最低）。

调节器：
  FlowLevel=SILENT → diff=0.6, scaffold=3, sub_step=3, error=5
  
降阶法 GOAL 阶段（scaffold=3，最长版本）：
"级数。听起来吓人——'无穷个数相加'。
但你已经会了数列，级数就是数列的前 n 项和取极限。

我们 3 个原子问题搞定它，每个都很简单。"

REDUCE 阶段（sub_step=3，最少原子）：
原子 1："级数和数列的区别？一句话。"
原子 2："无穷个数相加怎么可能得到有限值？像 1/2+1/4+1/8+...=1"
原子 3："判断级数收敛还是发散，最直觉的方法是什么？"

→ 前 2 个原子一次过，第 3 个错了。
   scaffold=3 行为：系统不推进，而是重述问题换角度：

"换一个问法：如果你有一个无穷级数，a₁+a₂+a₃+...，
它的第 n 项 a_n 在 n→∞ 时不趋近于 0——
这个级数有可能收敛吗？"

学生："啊——必须 a_n→0 才有可能收敛。
如果 a_n 不趋近 0，级数一定发散。"
→ correct，推进

→ 整体后 COMPLEMENT 阶段 error_count=5（最少，不 overwhelm）
→ 完成后骨架重构 → 学生信心恢复
→ flow_level 从 SILENT 升到 SHALLOW
→ 第 2 个概念起调节器升到 diff=0.8
```

---

## 5. 跨 Session 边界（第 1 天结束 → 第 2 天开始）

```
[第 1 天结束，Session 1 completed]

系统（调度器）：
"今天完成了 Phase 1（向量代数 skip）+ Phase 2 前半（偏微分学 7/20 概念）。
 
总结：
  净教学时间：3.2h
  概念学完：7 个
  平均 mastery：0.71
  心流记录：SHALLOW→FLUENT→DEEP（上升趋势良好）
  当前认知负荷：0.35（低，可以继续但今天配额已到）
  
明天计划：
  先 10 分钟复习今天到期概念（全微分、链式法则）
  然后继续第 8 章余下 13 个概念
  预计 3.5h
  然后开始第 9 章重积分（Phase 3）

还有什么想在你离开前提的吗？"

学生："没有，明天继续。"
```

```
[第 2 天，Session 2 恢复]

系统（resume_plan）：
"欢迎回来。先做 10 分钟到期限复习。
 
L3 遗忘曲线检测：
  全微分（18h 前学）→ recall = 0.73（到期）
  链式法则（16h 前学）→ recall = 0.76（接近到期）
  方向导数（14h 前学）→ recall = 0.81（尚好）

复习计划：
  全微分：quick_quiz（30s × 3 问）
  链式法则：concept_map（2min 画结构）
  总复习时间：8min

开始复习？"
```

---

## 6. 数据流汇总（优化后系统全貌）

```
                          ┌──────────────┐
                          │   学习计划    │
                          │ LearningPlan  │
                          │ ·5 Phase      │
                          │ ·6 Session    │
                          │ ·里程碑追踪   │
                          └──────┬───────┘
                                 │ resume_plan()
                                 ▼
  ┌─────────────────────────────────────────────────────┐
  │  TeachingEngine                                      │
  │                                                      │
  │  next_action()                                       │
  │  ├─ strategy_selector.select_strategy()              │◀── LearnerProfile
  │  │   ├── 规则 8（新增）: cognitive_load>0.6 → 降阶法 │      BKT mastery
  │  │   └── ...7 条原有规则                               │
  │  ├─ flow_regulator.recommend_pace()  ◀── FlowLevel  │◀── flow_tracker
  │  │   └─ PaceConfig {diff, scaffold, sub, err}       │
  │  ├─ get_strategy(name)                               │
  │  │   ├── 降阶法: GOAL→REDUCE→COMPLEMENT→RECONSTRUCT  │
  │  │   └── 其他: feynman/socratic/...                  │
  │  └─ _llm_generate_content(type, concept, pace)      │◀── LLM API
  │                                                      │
  │  respond()                                           │
  │  ├─ LLMScorer.score()                                │
  │  ├─ BKT.update(correctness)                          │
  │  ├─ ErrorDiagnoser.diagnose()                        │
  │  ├─ strategy.transition("answered", correctness)     │
  │  ├─ FlowSignals → flow_level update                  │
  │  ├─ gain_loop_monitor.detect_break()                 │
  │  ├─ _estimate_cognitive_load()                       │
  │  └─ _build_feedback(L1→L2→L3+firewall)              │
  │                                                      │
  │  handle_interrupt()                                  │
  │  ├─ save_checkpoint()                                │
  │  ├─ intent_classifier.classify()                     │
  │  └─ LLM 生成响应                                      │
  └─────────────────────────────────────────────────────┘
```

---

## 7. 48 小时验收清单（更新版）

```
┌───────────────────────────────────────────────────────────────┐
│  □ 冷启动摸底 ≤ 1h                                            │
│  □ KG 构建（800 概念）≤ 25min                                  │
│  □ 教学吞吐 ≥ 10 概念/小时（含降阶法 4 步完整流程）                │
│  □ 降阶法 4 步完整执行率 100%（GOAL→REDUCE→COMPLEMENT→RECONSTRUCT）│
│  □ 每概念产出知识骨架（前提→逻辑→结论→易错点）                      │
│  □ 心流调节器主动调参在一次教学动作前完成                          │
│  □ 心流断裂（FlowLevel.SILENT 持续 > 3 步）≤ 3 次 / 全科目       │
│  □ 增益回路检测+修复成功率 ≥ 80%                                 │
│  □ 里程碑完成时章节平均 mastery ≥ 0.70                            │
│  □ 跨 Session 恢复 ≤ 30s（含到期复习）                            │
│  □ 最终整体 mastery > 0.7                                        │
│  □ 计划偏差 |actual-predicted| / predicted ≤ 30%                │
│  □ LLM API 中断降级不停机                                         │
│  □ 降阶法与心流调节器可独立调用（模块分离）                          │
└───────────────────────────────────────────────────────────────┘
```

---

*本模拟基于以下组件已实现：降阶法策略（JiangjieStrategy）、心流调节器（FlowRegulator）、全部 10 项优化方向。  
降阶法策略的实现规格见 `ai-tutor-system-design/specs/jiangjie-strategy.md`，心流调节器代码见 `servers/tutoring_mcp/flow_regulator.py`。*
