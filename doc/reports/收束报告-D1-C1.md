# 收束报告 · D1 收束节点（C1 第一波）

> 触发：C1 完成（后端骨架 + API 网关 + 前端入口齐备）。区间：B 阶段收尾后 → C1（commit `678eda9..ef49a8d` + 本次 D1 审计修复）。
> 执行日期：2026-06-13 · 执行：AI（四阶段）· **签核：待人审核**（08-汇报收束 §6.7：AI 不替人签核）。
> 流程依据：`开发规范/.../08-汇报收束.md §六` 四阶段。

---

## 结论先行

C1 三模块（M-001/M-002/M-003）四阶段执行**全部通过**，期间审计阶段实查出 6 个依赖漏洞并当场修复。
**唯二未达项均为环境受限、非质量缺陷**：Electron 运行时未真跑（无桌面环境）、redocly 文档完整性告警（health-only 骨架的预期）。建议**签核通过 → 进 C2**。

---

## 阶段一 · 整理

| 检查项 | 结果 |
|--------|------|
| 代码 diff 汇总 | C1 新增 `backend/`（16 文件）+ `frontend/`（5 文件）+ 配置；D1 审计追加 starlette/pyjwt 安全下限。净增 ~790 行。**未触 data/ 演示资产** |
| worklog 完整性 | ✅ C1 有独立 worklog `2026-06-13_C1第一波-…md`（无多功能点共用）|
| 文档同步 | ✅ backend 实现匹配 BDD 01（health/版本/建表/信封）+ 02（网关/信封映射）；偏差已显式标注（32 tool 迁移延后 C2-C4）|
| STATUS 一致性 | ✅ 功能点表 #1✅/#2◐/#4◐ + 阶段 C·C1✅ 已更新 |
| ADR 归档 | ✅ ADR-0001（目录名 backend/）+ ADR-0002（backend.core 桥接 shared/）已落 `worklogs/decisions/` |

## 阶段二 · 测试

| 检查项 | 命令 | 结果 |
|--------|------|------|
| 单元 + 集成全量回归 | `pytest` | **810 passed**（797 基线 + 13 新 backend，0 回退）|
| 集成测试 | （本项目无 `@pytest.mark.integration` 标记）| 集成用例随全量跑（`tests/integration/` 在内）|
| 覆盖率（C1 新代码）| `pytest backend/ --cov=backend` | **92%**（>80% 门禁）。未覆盖：app.py db 初始化失败分支 + 兜底异常处理器 + `main.py` `__main__` 入口（已由真机 uvicorn 启动验证）|

> 注：全仓覆盖率门禁对棕地 v1 代码不适用，故只对 C1 新增 `backend/` 计覆盖率。

## 阶段三 · 审计

| 检查项 | 依据/工具 | 结果 |
|--------|----------|------|
| 规范红线（print/SQL/吞异常/断点）| ruff（T20/T100/B/E722…）| ✅ All checks passed |
| 架构红线（跨层/循环依赖）| import-linter | ✅ **3 kept 0 broken**（新增 backend.core ⊥ 上层契约）|
| 密钥泄露 | gitleaks（pre-commit）| ✅ Passed |
| **依赖漏洞** | `pip-audit` | 🔴→✅ 初扫 **6 漏洞 2 包**：starlette 1.0.0（PYSEC-2026-161）/ pyjwt 2.12.1（PYSEC-2026-175/177/178/179）→ 钉下限 starlette≥1.0.1（实装 1.3.1）、pyjwt≥2.13.0 → **复扫 No known vulnerabilities** |
| API 契约 | redocly lint（FastAPI 生成的 OpenAPI）| ⚠️ 6 errors/6 warnings，**全为文档完整性**（缺 license/4xx 响应文档/server 等），非正确性问题；C2 真 endpoint 落地时补全 |

## 阶段四 · 验证

| 检查项 | 结果 |
|--------|------|
| 接口真实调用 | ✅ `python -m backend.main` → `Uvicorn running on http://127.0.0.1:18501`；`curl /api/knowledge/health` → `{"ok":true,"version":"2.0.0","engine":"knowledge"}`；未知路由 → 404 |
| 真机 E2E（浏览器/Electron）| ⚠️ **未真跑**：`node --check` 过（main.js/preload.js 语法）、package.json 合法；Electron `npm start` 需 `npm install`（拉较大 electron 包）+ 无桌面环境，留待本地真验 |
| 截图存档 | N/A（后端为 JSON API，无页面；Electron 未运行）|

## 汇总产出

| # | 产出 | 落点 |
|---|------|------|
| 1 | 收束报告 | 本文件 `doc/reports/收束报告-D1-C1.md`（repo 沿用 .md，非 08 的 HTML）|
| 2 | ADR | `worklogs/decisions/0001-后端目录名-backend.md` + `0002-backend-core-桥接shared.md` |
| 3 | STATUS 更新 | 收束节点历史 + 功能点状态 |
| 4 | 开发清单 | C1 状态（见 STATUS 阶段 C）|

## 任务清单

- **完成**：M-001 服务入口（:18501 跑通 + 建库）/ M-002 API 网关（信封+异常映射+四引擎 health）/ M-003 客户端入口骨架 / 6 依赖漏洞修复 / 2 ADR。
- **未完成（按计划延后，非阻塞）**：#2 的 32 tool→REST/WS 完整迁移（C2-C4 随引擎）/ spec 04 前端 5 页面·向导·WebSocket（后续波次）/ OpenAPI 文档完整性补全（C2）。
- **阻塞**：无。
- **后续方向**：① Electron 运行时真验（本地桌面环境）；② C2 波前文档去 sys.path 化（走向决议 §五-6）；③ C2 第二波：M-004/005/006/007 + M-014——届时可打"v2 第一波过堂"挑战（异步 build_kg + 缓存命中 + 进程重启会话可续）。

---

> 待人签核：审本报告后在 STATUS「收束节点历史」把 D1 标 ✅ 已审计。
