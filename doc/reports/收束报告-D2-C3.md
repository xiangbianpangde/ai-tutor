# 收束报告 · D2 收束节点（C3 第三波·核心引擎就位）

> 触发：C3 完成（M-008 RAG 引擎 + M-009 教学编排齐备 → 核心引擎就位）。
> 区间：C2 收尾（`5c588b3`）→ C3（`88b8031` M-008 + `1668490` M-009 + 本次 D2 文档）。
> 执行日期：2026-06-15 · 执行：AI（四阶段）· **签核：待人审核**（08-汇报收束 §6.7：AI 不替人签核）。
> 流程依据：`开发规范/.../08-汇报收束.md §六` 四阶段。

---

## 结论先行

C3 两模块（M-008/M-009）四阶段执行**全部通过**，**无未达项**。885 测试全绿（855→885，
+30，0 回退）；backend 覆盖率 96%；审计四工具全过（含 pip-audit 0 漏洞）；真机 8 条新路由
全注册 + curl 验证 RAG 验证端点行为正确。建议**签核通过 → 进 C4**。

本节点关键决策已落 **ADR-0003**（RAG 务实落地，不引 ChromaDB/NLI 模型——死代理阻塞 +
当前规模过度工程；ChunkSource/judge 注入点留好升级路）。

---

## 阶段一 · 整理

| 检查项 | 结果 |
|--------|------|
| 代码 diff 汇总 | 新增 `backend/rag/`（7 文件）+ `backend/teaching/`（2 文件）+ 2 测试文件 + pyproject（ruff 免疫调用 + 2 契约）。净增 ~900 行。**未触 data/ 演示资产** |
| worklog 完整性 | ✅ 独立 worklog `2026-06-15_C3第三波-M008-RAG-M009-教学编排.md` |
| 文档同步 | ✅ 实现匹配 FF-M-008 状态机（IDLE→…→VALIDATED/CANNOT_CONFIRM）+ MD-M-008；务实偏差（词法检索替 ChromaDB / 启发式替 NLI）显式记 ADR-0003，升级路保留 |
| STATUS 一致性 | ✅ 功能点 #9◐/#10◐ + 阶段 C·C3✅ + 收束节点 D2 已更新 |
| ADR 归档 | ✅ ADR-0003（RAG 务实落地）已落 `worklogs/decisions/` |

## 阶段二 · 测试

| 检查项 | 命令 | 结果 |
|--------|------|------|
| 单元 + 集成全量回归 | `pytest` | **885 passed**（855 基线 + 30 新：RAG 21 + 教学 9，0 回退）|
| 覆盖率（C3 新代码）| `pytest backend/ --cov=backend` | backend TOTAL **96%**（>80% 门禁）。新模块：rag/routing 100% · rag/retriever 99% · rag/validator 98% · rag/engine 87% · teaching/orchestrator 88% |

> 未覆盖行均为防御性兜底（缓存反序列化异常分支 / 事件总线 publish 失败 swallow / Pydantic
> 非模型分支），非业务逻辑缺口。

## 阶段三 · 审计

| 检查项 | 依据/工具 | 结果 |
|--------|----------|------|
| 规范红线（print/SQL/吞异常/断点）| ruff（T20/T100/B/E…）| ✅ All checks passed（全仓）|
| 架构红线（跨层/循环依赖）| import-linter | ✅ **7 kept 0 broken**（C3 新增 backend.rag ⊥ 上层、backend.teaching ⊥ 上层 2 契约）|
| 密钥泄露 | gitleaks（pre-commit）| ✅ Passed（M-008/M-009 两次提交均过钩子）|
| 依赖漏洞 | `pip-audit` | ✅ **No known vulnerabilities found**（仅本地包 ai-tutor/research-tool 因不在 PyPI 跳过）|
| 反幻觉红线 | 代码审查 | ✅ 验证回退启发式**永不过度授信**（置信封顶 0.6 / 无源不支撑），单测专项守护（`test_validator_heuristic_never_overclaims_confidence`）|

## 阶段四 · 验证

| 检查项 | 结果 |
|--------|------|
| 路由注册 | ✅ `python -m backend.main` → `/openapi.json` 含全部 8 条新路由（rag/query·rag/validate + sessions/start·{id}/next-action·respond·advance·transition）|
| 接口真实调用 | ✅ curl `POST /api/knowledge/rag/validate`：支撑案例 → `{is_supported:true, confidence:0.6, method:heuristic}`（启发式封顶生效）；无源案例 → `{is_supported:false, confidence:0.0, mismatched_claims:["无引用源"]}` |
| RAG 检索 / 教学编排端到端 | ◐ 单测全链覆盖（注入 KG 概念 / FakeEngine）；真 KG + 真 LLM 的端到端留待 C4 后牺牲品语料 demo（资产保护红线：不碰 fastapi-houduankaifa）|

## 汇总产出

| # | 产出 | 落点 |
|---|------|------|
| 1 | 收束报告 | 本文件 `doc/reports/收束报告-D2-C3.md` |
| 2 | ADR | `worklogs/decisions/0003-RAG务实落地-不引ChromaDB与NLI模型.md` |
| 3 | worklog | `worklogs/2026-06-15_C3第三波-M008-RAG-M009-教学编排.md` |
| 4 | STATUS 更新 | 收束节点历史 D2 + 功能点 #9/#10 + 阶段 C·C3 |

## 任务清单

- **完成**：M-008 RAG 引擎（词法检索 + NLI 风格验证 + 自主重检索状态机 + REST）/ M-009 教学
  编排（v1 引擎 → REST 会话驱动 + M-004/M-006 接线）/ ADR-0003 / 2 架构契约。
- **未完成（按计划延后，非阻塞）**：真向量后端（ChromaDB）+ 真 NLI 模型（设计目标态，规模化阶段）；
  RAG/教学真 KG+LLM 端到端 demo（C4 后牺牲品语料）；#10 其余拆分模块（费曼/FSRS/阶段校准/
  长期记忆/红线编排 = C4）。
- **阻塞**：无。
- **后续方向**：C4 第四波 M-010~M-013 + M-016。

---

> 待人签核：审本报告后在 STATUS「收束节点历史」把 D2 标 ✅ 已审计。
