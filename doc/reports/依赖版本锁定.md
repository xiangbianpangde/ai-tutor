# 依赖版本锁定记录（B3）

> 更新：2026-06-13 · 阶段 B 收尾。来源：`uv.lock` 实测 + 走向决议 §五-2 + memory dead-system-proxy。
> 口径：**当前已锁** = 已在 `pyproject.toml` 依赖且 `uv.lock` 有 pin；**C 波新增** = 动工对应波次时
> `uv add` 锁定，现在不提前引入（避免未用依赖 + 锁面膨胀）。

## 一、当前已锁（uv.lock 实测）

| 包 | 锁定版本 | 用途 | 许可证/备注 |
|------|---------|------|------------|
| fastmcp | 3.3.1 | v1 MCP 框架（v2 迁 FastAPI 后逐步退场）| MIT |
| pydantic | 2.13.4 | 60+ schema 数据建模 | MIT |
| sqlalchemy | 2.0.49 | 14 ORM 表持久化 | MIT |
| openai | 2.37.0 | LLM 兼容客户端（DeepSeek/OpenAI）| Apache-2.0 |
| pymupdf | 1.27.2.3 | PDF 文本提取 | AGPL-3.0 ⚠️ 仅库内调用，打包须复核（见 §三）|
| ruff | 0.15.13 | lint + format（pre-commit 门禁）| MIT |
| import-linter | 2.11 | 架构契约（shared⊥servers / 四 server 独立）| BSD-2 |
| pre-commit | 4.6.0 | 提交前门禁编排 | MIT |
| grimp | 3.14 | import-linter 的依赖图后端 | BSD-2 |

> 本日新增 `import-linter` / `pre-commit`（连同 grimp 等传递依赖）已 `uv lock` 折入，无既有 pin 漂移。

## 二、C 波动工时新增（现在**不**引入）

| 包 | 目标版本 | 引入波次 | 决策来源 |
|------|---------|---------|---------|
| fastapi | 0.115.x | C1（M-001/M-002）| 走向决议 §三 技术栈显式化采纳；**注意**：venv 现存 fastapi 0.115.5 系历史遗留，既不在 pyproject 也不在 uv.lock，C1 动工时正式 `uv add fastapi==0.115.*` |
| uvicorn | 配合 fastapi | C1 | ASGI server |
| sqlite-vec | 0.1.x（锁死）| C2/C3（M-005/M-008）| TD-001 + 走向决议 §二-5：API 未稳定，锁补丁位 + `VectorIndex` 抽象层 + ChromaDB 0.5.x 备选；季度复审上游（E3）|
| langgraph | 1.0.x | C3（M-008 落地）| 走向决议 §三 |

## 三、外部工具（subprocess 隔离，**不进** pyproject 依赖）

| 工具 | 实测版本 | 调用方式 | 许可证红线 |
|------|---------|---------|-----------|
| mineru | **3.2.0** | CLI 子进程（PDF→Markdown / OCR）| 上游 **MinerU AGPL-3.0**；进程隔离、不捆绑。**M-015 打包红线：不得把 mineru 捆进安装包**，引导用户自装；商业化/捆绑分发前对当时版本许可证做法务复核（走向决议 §二-4）|
| md_translator | 树内 MIT | 库内 import（`servers/knowledge_mcp/md_translator.py`）| 用户自有 MIT（Copyright 2026 xiangbianpangde），← `C:\Users\yhn\pdf2zh\translate_md.py`；**上游 AGPL 的 pdf2zh 1.9.6 包已移出依赖**——B3 锁定对象由 pdf2zh 改为 mineru |

## 四、环境注意

- **死系统代理**（127.0.0.1:7897）会让 `uv` / mineru / DeepSeek 全链 10054——`uv lock`/装包/教学演示前设进程级 `NO_PROXY=*`（见 memory `dead-system-proxy-workaround`）。
- pymupdf 与 mineru 均涉 AGPL，二者都只在进程内/子进程使用、不随发行版分发；C5 打包评审须把这两项列入法务清单。
