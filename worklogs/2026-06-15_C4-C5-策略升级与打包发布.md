# 2026-06-15 · C4 第四波 + C5 第五波——策略升级 + 打包发布

> 区间：D2 收束（ca1bbee）→ C4（e1aa27d）→ C5（1c2af16）。对应 v2 #10 拆分 + #11 打包发布。
> 934 测试全绿（855→934，+79，0 回退）。**v2 11 功能点全部就位**。

---

## C4 第四波（M-010~M-013 + M-016，commit e1aa27d）

策略升级（#10 拆分），5 模块务实落地，重依赖纯 Python 替代（ADR-0004）：

- **M-010 费曼评分**（`backend/strategy/feynman.py`）：概念关键点覆盖率 + 背书检测（逐字抄定义
  → is_parroting）；LLM 法官可注入，回退启发式诚实封顶 0.6。#12
- **M-011 FSRS-5**（`backend/strategy/fsrs.py`）：纯 Python 实现 FSRS-5（19 权重双状态），替
  py-fsrs；`get_default_scheduler` 单例。遗忘曲线现代升级。
- **M-012 阶段校准**（`backend/strategy/stage_calibration.py`）：滑动窗口 + 滞回防抖，连续达标
  才升降档。#16 阶段过渡太猛
- **M-013 长期记忆**（`backend/memory/longterm.py`）：已确认事实落库长存 + LRU 淘汰 + service
  Façade；token 重叠检索接地。#18 长期幻觉
- **M-016 红线编排**（`backend/redline/`）：ruff/import-linter 等收进可编程入口聚合 ToolResult；
  执行器可注入。
- REST：`/api/tutoring/{feynman/score,review/schedule,stage/calibrate,memory/facts}` + `/api/meta/redline`
- 36 测试。

## C5 第五波（M-015 + M-001 收尾，commit 1c2af16）

打包发布（#11），完成 v2 最后一个功能点：

- **M-015 打包调度**（`backend/build/`）：BuildOrchestrator 四步 FSM（NUITKA→UPX→VERIFY→DONE）
  + NuitkaBuilder（**--standalone onedir，硬红线禁 --onefile**，R-01）+ UPXCompressor（软降级
  仅 WARN）+ ArtifactVerifier（exe 硬 / 体积软）；执行器可注入。
- **M-001 收尾**：`GET /api/meta/health` 聚合各子系统就绪态（db/cache/sessions/tasks/events）。
- REST：`POST /api/meta/build`。
- 打包设计文档修正：PyInstaller onefile → Nuitka onedir + extraResources 整目录引用 + 风险表更新。
- 13 测试。

## 验证（D3 收束四阶段，详见 `doc/reports/收束报告-D3-C5.md`）

- 测试：934 passed（+79，0 回退）；backend 覆盖率 96%
- 审计：ruff All passed / import-linter 8 kept 0 broken / pip-audit 0 漏洞 / gitleaks pass
- 真机：`python -m backend.main` → `GET /api/meta/health` 五子系统全 up；25 条路由注册

## 里程碑

**v2 11 功能点全部就位**（实现层面）。下一步 = D3 收束节点（本波触发）→ 后续真机端到端
demo（牺牲品语料）+ 前端 5 页面/向导/WebSocket（spec 04 后续波次）+ 真 Nuitka 编译发布。
