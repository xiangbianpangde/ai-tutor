# 2026-06-15 · 前端补全——5 页面 + 可视化 + WebSocket（#4/#5/#7/#8）

> 区间：D3 收束（a8bffc0）→ #7 WS（69ad4c7）→ 前端（fdf3bae）。
> 补齐 v2 仅剩的前端层：#4 前端应用 / #5 可视化 / #7 实时推送 / #8 首次向导。

---

## 后端：#7 WebSocket 实时推送（commit 69ad4c7）

- `/ws/events`：accept → 订阅 M-006 EventBus 全部事件（通配 `*`）→ asyncio.Queue 解耦 →
  实时 send_json。教学/任务/RAG 动作 publish 的事件实时转发，前端免轮询。
- 3 测试（握手 + 经 HTTP 触发实时收到 teaching/rag 事件）。

## 后端：可视化数据端点（commit fdf3bae）

- `GET /api/knowledge/graphs/{kg_id}/view`：只读返回概念节点（含 category/abstract_level/load）+
  边，供力导向图/热力图渲染。1 测试。

## 前端：React 渲染层（commit fdf3bae）

`frontend/renderer/`（Vite + React，与 Electron 外壳分离让 install 轻）。**Vite build 通过**
（78 模块 / 66KB gzip）：

- **5 页面 + 侧边栏路由（#4）**：仪表盘（系统健康 + 缓存命中 + 实时事件流）/ 学习中心
  （开会话 → 对话式作答 → 实时反馈）/ 知识图谱 / 复习 / 设置（LLM 配置 + 偏好，localStorage）
- **首次向导（#8）**：3 步可跳过 LLM 配置
- **WebSocket 客户端（#7 前端侧）**：`useWebSocket` hook，指数退避重连（spec 04 功能4 场景2）
- **3 可视化（#5）**：ForceGraph（d3-force 力导向图，category 着色，点击详情）/ ForgettingCurve
  （FSRS 保持率公式同后端，多 stability 曲线）/ MasteryHeatmap（认知负荷热力图）
- `api.js` 统一信封解包；Electron `main.js` 加载 `renderer/dist`（`AITUTOR_RENDERER_URL` 可切 dev server）

## 验证：Playwright 真浏览器全栈打通

启真后端（:18501）+ 静态服 dist（:4173），Playwright 实测：
- 仪表盘渲染 + **拉真后端 `/api/meta/health` → 子系统 5/5**、缓存命中率、版本 2.0.0
- 侧边栏「实时已连接」+ 事件流收到 **`ws.connected {bus:true}`**（真 WebSocket）
- 首次向导弹出（localStorage 未 onboarded）
- 复习页：FSRS 评分按钮 + 遗忘曲线 SVG（轴/90% 目标线/3 曲线）；**点「良好」→ 真后端
  FSRS 返回 → 「下次 3 天后 · S=3.2」**（前后端 FSRS 往返打通）
- 仅 favicon 404（cosmetic，Electron 内无影响）

> 这超出 D1「Electron 运行时未真跑」的状态：渲染层 + WebSocket + 后端集成已在真浏览器验证。
> 仅剩 Electron 外壳运行时真跑（需桌面环境，环境受限非缺陷）。

## 里程碑

**v2 11 功能点全部就位且前端全栈打通**。剩余 = 真机端到端 demo（牺牲品语料）+ Electron
运行时真跑 + 真 Nuitka 编译 + 终极验收 #21。
