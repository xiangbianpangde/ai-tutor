{
  "name": "ai-tutor-desktop",
  "version": "2.0.0",
  "private": true,
  "description": "AI-Tutor 桌面客户端（Electron 外壳 + spawn Python 后端）。C1 仅入口骨架，5 页面/向导/WebSocket 见后续波次。",
  "license": "MIT",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js"
  },
  "devDependencies": {
    "electron": "^33.0.0"
  }
}
